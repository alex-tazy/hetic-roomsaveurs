{% extends 'HETICAppBundle::layout.html.twig' %}

{% block template %}

    {% if app.request.hasPreviousSession %}
        {% for type, messages in app.session.flashbag.all() %}
            {% for message in messages %}
                <div class="flash-{{ type }}">
                    {{ message }}
                </div>
            {% endfor %}
        {% endfor %}
    {% endif %}

<header>
    <div class="logo"></div>
    <nav class="menu">
        <span class="itemMenu itemMenu1"><a href="#">Se connecter</a></span>
        <span class="itemMenu itemMenu2"><a href="#">S'inscrire</a></span>
        <span class="itemMenu itemMenu3"><a href="#">Mon panier</a></span>
   </nav>
   <div class="burger-menu">
        <div class="burger"></div>  
    </div>
</header>

{% block main %}
{% endblock main %}

<footer>
    <div class="container">
        <div class="row">
            <div class="col-sm-12">
                <a href="{{path('hetic_app_homepage')}}">
                    <img src="{{asset('bundles/heticapp/img/logo_RS.png')}}" alt="logo_footer" class="logo">
                </a>
            </div>
        </div>
        <div class="row">
            <div class="col-sm-12">
                <nav>
                    <ul>
                        <li><a href="#">Contact</a></li>
                        <li><a href="#">Mentions légales</a></li>
                        <li><a href="#">Se connecter</a></li>
                        <li><a href="#">S'inscrire</a></li>
                    </ul>
                </nav>
            </div>
        </div>
        <div class="row">
            <div class="col-sm-12">
                <nav class="socials">
                    <ul>
                        <li><i class="fa fa-facebook"></i></li>
                        <li><i class="fa fa-instagram"></i></li>
                        <li><i class="fa fa-twitter"></i></li>
                    </ul>
                </nav>
            </div>
        </div>
    </div>
</footer>

{% endblock template %}
